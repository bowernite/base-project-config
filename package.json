{
  "scripts": {
    "//": "REVIEW: `--ext` eslint list",
    "lint": "eslint . --ext js,jsx,ts,tsx && prettier --check .",
    "lint-fix": "eslint . --fix --ext js,jsx,ts,tsx && prettier --write --loglevel=error ."
  }
}
